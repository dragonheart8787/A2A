# AI 產線治理（AI Governance）專案報告

---

## 一、動機

在 AI 輔助開發日益普及的今日，多個 AI 代理協作、迭代同一程式碼庫時，常面臨以下問題：

- **缺乏可追溯性**：後續 AI 無法得知前一個 AI 做了什麼、為何這樣做
- **Scope Creep**：需求與複雜度不斷膨脹，缺乏明確邊界
- **重寫風險**：AI 可能擅自重寫既有穩定模組，破壞相容性
- **變更失控**：缺乏可比較、可回滾、可審核的變更紀錄

因此，本專案旨在建立一套 **可機讀、可驗證、可繼承的 AI 工程契約**，讓 AI 之間能「對接工作」而非各自為政，同時透過硬治理（pre-commit、CI）確保規則真正生效。

---

## 二、過程

### 2.1 架構設計

1. **契約層（contract.md）**：定義專案範圍、不可侵犯原則、Gates
2. **架構層（architecture.md、modules.yaml）**：模組邊界、public interfaces、data schema
3. **決策帳本（decisions/）**：ADR 機讀化，記錄重要決策
4. **變更控制（workflow.yaml、change_control.md）**：狀態機 Idea → Approved → Released
5. **預算與 Gates（budgets.yaml）**：複雜度預算、單次變更上限
6. **互通層（interop/）**：AI↔AI 身份、工單、工件、協作、衝突、安全

### 2.2 實作步驟

| 階段 | 內容 |
|------|------|
| 規範定義 | 撰寫 contract、architecture、modules、workflow 等 YAML/MD |
| Schema 設計 | 為各 spec 區塊建立 JSON Schema，供 CI 驗證 |
| 驗證腳本 | `validate_schemas.py`、`validate_interop.py`、`enforce.py` |
| 硬治理整合 | pre-commit hooks、GitHub Actions CI |
| Interop 擴充 | 六面向基礎 + 八項企業級升級（Policy-as-Code、No Rewrite 語意等） |

### 2.3 執行流程

AI 執行變更前必須：

1. 讀取 contract、modules、workflow
2. 產出結構化變更提案
3. 通過 Gates（Reuse-first → Diff-only → Complexity Budget → Evidence-based）
4. 確認 workflow 狀態為 Approved
5. 完成後在 change_log.jsonl 追加記錄

---

## 三、摘要

本專案建立了一套完整的 **AI 產線治理框架**，包含：

- **軟引導**：`.cursor/rules/ai-governance.mdc` 提示工程
- **硬治理**：pre-commit + CI，違規 commit/PR 無法通過
- **五條 MVP 校驗**：change_log 必填、touched_files 與 git diff 一致、stable 模組 diff 行數限制、依賴白名單、breaking change 需 rollback_plan
- **AI↔AI 互通**：Agent Identity、Task Ticket、Artifact Registry、Coordination Protocol、Conflict & Ownership、Security/Trust，以及 Policy-as-Code、No Rewrite 語意等企業級擴充

所有規格均具備 JSON Schema，可機驗；變更具可追溯、可回滾、可審核特性。

---

## 四、心得反思

### 4.1 收穫

- **系統化思維**：將「人與 AI 的默契」轉為可執行的契約與規則，降低溝通成本
- **分層治理**：契約 → 架構 → 決策 → 變更控制，層次分明，便於維護
- **機讀優先**：YAML/JSON + Schema 讓 AI 與工具都能理解與驗證，避免歧義
- **軟硬並行**：既有提示工程引導，又有 CI 強制，兼顧彈性與紀律

### 4.2 挑戰與取捨

- **過度規範 vs 靈活開發**：規則過嚴可能拖慢迭代，需在 budgets、allowed_changes 上持續調校
- **Schema 維護成本**：每次擴充 spec 都要同步更新 JSON Schema，需建立習慣
- **跨代理協定**：Interop 設計需考慮不同 AI 系統的實作差異，兼容性測試重要

---

## 五、總結和未來想加強的領域

### 5.1 總結

本專案成功建立了一套可機讀、可驗證的 AI 產線治理體系，從契約、架構、決策到變更控制皆有明確規範，並透過 pre-commit 與 CI 落實硬治理。Interop 層則為多 AI 代理協作提供了共通語言，支援工單、工件、協調、衝突與安全等面向。

### 5.2 未來想加強的領域

| 領域 | 說明 |
|------|------|
| **A2A 相容性** | 與 A2A（Agent-to-Agent）標準對接，強化 transport/discovery 互通 |
| **Observability 實作** | 將 trace_id、span_id、decision_log 匯出至 OpenTelemetry/SIEM |
| **Policy-as-Code 自動化** | 自動產生 policy_context、policy_check_result，減少人工填寫 |
| **Objective Verification DSL** | 擴充驗收語言，支援更多 test_commands、metric_thresholds |
| **Provenance Chain** | 建立 SBOM-like 溯源鏈，追蹤 artifact  lineage |
| **預算與 Gates 動態調整** | 依專案規模、團隊習慣自動建議 budgets 與 allowed_changes |
| **PR Bot 智慧留言** | 當 enforce 失敗時，自動分析缺失並給出修正建議 |

---

## 六、製作過程和遇到的困難

### 6.1 製作過程

1. **需求釐清**：分析 AI 協作痛點，確立「可機讀、可驗證、可繼承」目標
2. **規格撰寫**：依層級撰寫 contract、architecture、modules、workflow
3. **Schema 設計**：為每個 spec 區塊設計 JSON Schema，確保結構一致
4. **腳本開發**：實作 validate_schemas、validate_interop、enforce
5. **CI 整合**：設定 pre-commit、GitHub Actions
6. **Interop 擴充**：設計六面向 + 八項企業級升級，撰寫 YAML 與 schema

### 6.2 遇到的困難

| 困難 | 說明 | 解決方式 |
|------|------|----------|
| **Spec 與 Schema 同步** | Markdown 內 YAML 與 JSON Schema 容易脫節 | 建立 version-matrix，CI 強制驗證；spec 區塊統一用 `# spec:<type> v<N>` 標註 |
| **Enforce 與 git diff 比對** | touched_files 需與實際變更一致 | 在 enforce.py 中解析 git diff，比對 change_log 的 touched_files |
| **Stable 模組 diff 限制** | 如何合理計算「單檔 diff 行數」 | 使用 `git diff --numstat` 取得行數，與 budgets.yaml 的 stable_module_max_diff_lines 比對 |
| **Interop 複雜度** | 六面向 + 八升級，檔案與關聯多 | 以 README 整理對照表，並提供 validate_interop 一鍵驗證 |
| **Breaking change 判定** | 何時需強制 rollback_plan | 在 change_control 中定義 breaking 條件（如 public API 變更、schema 不相容），CI 檢查 change_type |
| **依賴白名單維護** | 新增套件易遺漏登記 | 在 enforce 中比對 requirements/dependencies 與 dependency_allowlist，失敗時明確列出未登記項目 |

---

## 七、具體成果證據和案例

### 7.1 可執行的程式碼證據

#### 證據 1：Enforcement 腳本（`ai-governance/scripts/enforce.py`）

五條 MVP 校驗已實作為可執行的 Python 腳本，共 **278 行**，包含：

| 校驗項目 | 實作方式 |
|----------|----------|
| MVP 1：change_log 必填 | `get_change_log_new_entries()` 比對 base 與 HEAD，無新筆則 fail |
| MVP 2：touched_files 一致 | `logged != diff_set` 比對 change_log 與 `git diff --name-only` |
| MVP 3：stable 模組限制 | `get_diff_numstat()` 用 `git diff --numstat` 取得行數，與 `budgets.yaml` 比對 |
| MVP 4：依賴 allowlist | `detect_new_dependencies()` 解析 package.json/requirements.txt，比對 `dependency_allowlist` |
| MVP 5：breaking 須 rollback | 遍歷 `new_entries`，若 `breaking=true` 則檢查 `rollback_plan` 非空 |

**程式碼片段（第 232–268 行）：**

```python
# MVP 1: 所有變更必須新增一筆 change_log.jsonl
if not new_entries:
    errors.append("ENFORCE: 有程式變更但未在 ai-governance/change_log.jsonl 新增任何一筆記錄。")
# ...
# MVP 2: touched_files 必須與 git diff 一致
if logged != diff_set:
    if missing:
        errors.append(f"ENFORCE: touched_files 未包含下列實際變更檔案: {sorted(missing)}")
# MVP 3: 觸及 stable 模組禁止大改
if total > max_lines:
    errors.append(f"ENFORCE: 檔案 {path} 屬於 stability:stable 模組，單檔變更 {total} 行超過上限 {max_lines}。")
```

---

#### 證據 2：CI 自動化（`.github/workflows/ai-governance.yml`）

GitHub Actions 工作流程已設定，PR 不通過無法 merge：

- **Schema 驗證**：`validate_schemas.py` 驗證所有 spec 區塊
- **Enforcement**：`enforce.py` 執行五條 MVP 校驗，`GOVERNANCE_CI_BASE` 設為 PR base SHA
- **PR 自動留言**：enforce 失敗時，`actions/github-script` 自動在 PR 留言缺失項，引導修正

```yaml
# 關鍵步驟
- name: Enforcement（變更須有 change_log、touched_files 一致...）
  env:
    GOVERNANCE_CI_BASE: ${{ github.event.pull_request.base.sha || 'HEAD^' }}
  run: python ai-governance/scripts/enforce.py 2>&1 | tee enforce.log

- name: 於 PR 留言缺失項（僅在 enforce 失敗且為 PR 時）
  if: failure() && github.event_name == 'pull_request'
  uses: actions/github-script@v7
```

---

#### 證據 3：Pre-commit 本地攔截（`.pre-commit-config.yaml`）

```yaml
hooks:
  - id: ai-governance-schema
    name: ai-governance schema 驗證
    entry: python ai-governance/scripts/validate_schemas.py
  - id: ai-governance-enforce
    name: ai-governance 五條 MVP 校驗
    entry: python ai-governance/scripts/enforce.py
```

commit 前自動執行，違規 commit 直接擋掉。

---

### 7.2 機讀 Schema 證據

#### 證據 4：Change Log Schema（`ai-governance/schemas/change-log-entry.json`）

每筆變更必須符合 JSON Schema，含條件邏輯：

```json
{
  "required": ["schema", "change_id", "timestamp", "change_type", "breaking", "touched_files", "rationale", "impact_areas"],
  "properties": {
    "change_type": { "enum": ["bugfix", "refactor", "feature", "security", "perf"] },
    "impact_areas": { "items": { "enum": ["API", "data", "performance", "security"] } }
  },
  "if": { "properties": { "breaking": { "const": true } } },
  "then": { "required": ["rollback_plan"], "properties": { "rollback_plan": { "minLength": 1 } } }
}
```

**效果**：`breaking: true` 時，`rollback_plan` 必填且不可為空字串。

---

#### 證據 5：Task Ticket Schema（`ai-governance/interop/schemas/task-ticket.json`）

AI 工單的完整 schema，定義 `owner_agent`、`consumer_agent`、`objective`、`acceptance_criteria`、`deliverables`、`handoff_protocol` 等欄位，並支援 `acceptance_verification`（test_commands、metric_thresholds、regression_scenarios）。

---

### 7.3 實際案例

#### 案例 1：Task Ticket 範例（`interop/tasks/task-example.yaml`）

```yaml
task_id: TASK-001
owner_agent: orchestrator-agent
consumer_agent: example-code-agent
objective: "在 data_loader 模組新增 load_prices(symbols, start, end) 的單元測試，覆蓋率 ≥ 80%。"
acceptance_criteria:
  - "tests/data_loader/test_load_prices.py 存在且通過"
  - "coverage 對 data_loader 模組 ≥ 80%"
  - "未修改 public API 簽名"
inputs:
  - artifact_id: spec-api-canon
    ref: "ai-governance/architecture.md#api-canon"
  - artifact_id: module-data-loader
    ref: "ai-governance/modules.yaml#data_loader"
constraints:
  forbidden_changes: ["src/api/*", "modules.yaml"]
  risk_level: low
deliverables:
  - type: file
    path_or_schema: "tests/data_loader/test_load_prices.py"
    required: true
handoff_protocol: patch
```

**說明**：工單明確定義目標、驗收條件、輸入引用、禁止變更範圍、交付物，防止 scope creep。

---

#### 案例 2：Agent Identity 範例（`interop/agents/example-agent.yaml`）

```yaml
agent_id: example-code-agent
capabilities: [code_review, backend_impl, spec_writing, patch_generation]
constraints:
  - "不修改 stability: stable 模組的 public API"
  - "不新增未在 dependency_allowlist 的依賴"
  - "僅在 coordination 狀態為 approved 時執行寫入"
tools:
  filesystem: allowlist
  allowlist_paths: ["src/", "tests/", "ai-governance/"]
  network: outbound_allowlist
  network_allowlist: ["api.example.com"]
  sandbox: restricted
  tool_names: ["read_file", "search_replace", "run_tests"]
sla:
  response_deadline_seconds: 300
  max_context_tokens: 128000
```

**說明**：AI 身份與能力宣告，含權限、約束、工具白名單、SLA，可機讀驗證。

---

#### 案例 3：Coordination Protocol（`interop/coordination-protocol.yaml`）

狀態機：`propose → review → approve → execute → verify → close`

- **Handshake 規則**：B 回 accept + 預估輸入需求；A 提供缺的 inputs；B 才能動手
- **Policy Gate**：進入 execute 前必須 `policy_check_result.pass`
- **Work Contract 綁定**：message 須帶 `work_contract_id`，drift 則 reject
- **Delivery Envelope**：交付須帶 `delivery-envelope.json` schema

---

#### 案例 4：Budgets 具體數值（`ai-governance/budgets.yaml`）

```yaml
per_change_limits:
  new_files_max: 3
  new_dependencies_max: 1
  new_public_functions_max: 5
  cyclomatic_complexity_delta_max: 2
  new_lines_max: 300
  stable_module_max_diff_lines: 50   # CI 強制
global_budgets:
  total_dependencies_cap: 50
  api_surface_growth_per_release: 10
```

**說明**：數值化預算，enforce 腳本直接讀取並比對。

---

#### 案例 5：Workflow 狀態機（`ai-governance/workflow.yaml`）

```yaml
states: [Idea, Proposed, Approved, Implemented, Verified, Released]
ai_allowed_actions:
  - state: Approved
    allowed: [write_production_code]
    required: [must_follow_gates, must_log_to_change_log]
  - state: [Idea, Proposed]
    forbidden: [production_code]
rule: "非 Approved 的需求，AI 不得撰寫 feature 程式碼；僅能產出 spec / impact / plan。"
```

**說明**：AI 僅在 Approved 時可寫 production code，其餘狀態僅能產出 spec/plan。

---

### 7.4 檔案清單（可驗證的實體產出）

| 類別 | 檔案路徑 |
|------|----------|
| 契約與架構 | `contract.md`、`architecture.md`、`modules.yaml`、`workflow.yaml`、`budgets.yaml` |
| 變更控制 | `change_control.md`、`change_log.jsonl` |
| Schema | `schemas/change-log-entry.json`、`schemas/contract-spec.json` 等 |
| Interop Schema | `interop/schemas/task-ticket.json`、`agent-identity.json`、`delivery-envelope.json` 等 |
| 範例 | `interop/tasks/task-example.yaml`、`interop/agents/example-agent.yaml` |
| 腳本 | `scripts/enforce.py`、`validate_schemas.py`、`validate_interop.py` |
| CI/Pre-commit | `.github/workflows/ai-governance.yml`、`.pre-commit-config.yaml` |
| 軟引導 | `.cursor/rules/ai-governance.mdc` |

---

*報告完成日期：2025 年 3 月 14 日*
